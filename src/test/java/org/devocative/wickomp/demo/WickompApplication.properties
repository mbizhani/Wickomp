# Used in WDateInput
label.year=year
label.month=month
label.day=day
label.hour=hour
label.minute=minute
label.second=second

#Used in OBooleanFormatter
label.true=true
label.false=false

#Used in WSelectionInput
label.select=select
label.noOfSelection=noOfSelection

#Used in OGroupFieldButton
datagrid.groupField.select=select group field
datagrid.groupField.none=-- none ---

WAsciiIdentifierValidator=Invalid ASCII identifier value for '${label}'
WPatternValidator=Invalid pattern '${pattern}' for '${label}'
name.format=Invalid format for '${label}'